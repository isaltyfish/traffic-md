# 项目需求

1. 通过 tail 实时跟踪 nginx 日志中的 $bytes_sent 字段统计每个ip的流量消耗，如果ip很多（超过1000个），那么按照指定的淘汰策略(后面当度说明淘汰策略)来淘汰ip
2. 统计流经nginx的公网流量和本地流量（内网地址和回环地址），我希望有个函数来判读一个ip是公网还是内网，这是一个按天的聚合统计，仅分为公网流量、内网流量和其他流量（如果不能区分内网还是外网）。
3. 统计特定端点的流量，例如 '/abc/def/**' 的流量
4. 所有的统计要以天作为一个周期来统计
5. 统计总的 qps 和 特定端点的qps(例如 /abc/def/** 端点) ，注意按端点统计的时候，不是将统计精确到具体路由，而是划分到端点 pattern，端点pattern 在配置文件中配置的，可以参考 05.yaml。要求基于滑动窗口来实现，要求通过配置来支持同时追踪多个指标，例如同时支持5s、10s、1分钟的 qps。


## 淘汰策略

淘汰统计的维度也是按天的，所以不存在某个ip一直被保留。隔天所有的ip维度的统计全部重新开始。
ip 超过4000个触发淘汰策略。

1. 一个 ip 至少被保留15分钟不被淘汰
2. 淘汰应该优先淘汰流量小的，流量大的不应淘汰


## 集成 echo 框架

echo 框架地址 https://github.com/labstack/echo

我需要两个 web 端点，返回结果为json

1. /api/ip/{count}，该端点返回根据ip统计的结果，返回根据流量从到小的条目，每个条目包含 ip 和 firstSeen(格式化 HH:mm:ss)，流量（格式化为MB）
2. /api/endpoint/{count}，该端点返回根据端点统计的结果，要求包含端点和流量（格式化为MB）

count 是一个动态数字，返回指定条数的数据。

## 集成配置文件支持

我希望通过一个 app.properties 文件来配置web 端口和监控端点，同事我希望配置文件被打包到编译产物中。

## report 记录

每隔5分钟，记录流量统计结果到当前 exe 所在目录的下 stat.log 中。

## 日志模拟

按行读取 cmd/logsample.txt，并写入 /tmp/nginx.log 中，要注意读取和写入速度，模拟一个正常的 nginx 日志。
 